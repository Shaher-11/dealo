<div class="container mt-5 app text-center text-white">
    <div>
        <%= link_to 'javascript:history.back()' do %> <h4>
            <i class="fas fa-arrow-left"></i>Back</h4>
        <% end %>
        <h1><%= @group.name %> Group </h1>
    </div>
        
    <div>
        <div>
            <div>
                <p>Created At: <%= @group.created_at.strftime("%Y-%m-%d") %></p>
            </div>
            <p>Price: $<%= @group.total %></p>
        </div>
            </div>
    <div>
        <%= link_to 'Add Deal', new_group_deal_path(@group), class: 'btn btn-primary'  %>
    </div>
</div>
